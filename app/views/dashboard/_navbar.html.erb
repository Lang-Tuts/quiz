<div class="navbar navbar-default">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="javascript:void(0)">IIT Examination</a>
    </div>
    <div class="navbar-collapse collapse navbar-responsive-collapse">
        <ul class="nav navbar-nav">
            <% if user_signed_in? %>
            <li class="dropdown">
            <a href="bootstrap-elements.html" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Challange<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>
                <li>
                <%= link_to 'Challenge some one', challenge_path %>
                </li>
                <li>
                <%= link_to 'Participate to a challenge', participate_path %>
                </li>
                </li>
            </ul>
            </li>
            <% end %>
        <% if user_signed_in? %>
            <li class="dropdown">
            <a href="bootstrap-elements.html" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Create a challenge<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>
            <% if user_signed_in? %>
                <li>
                <%= link_to 'Configure exams', exams_path %>
                </li>
            <% end %>
                </li>
            </ul>
            </li>
        <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">


        <% if user_signed_in? %>
        <% if current_user.has_role? :admin %>
            <li class="dropdown">
               <a href="bootstrap-elements.html" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Questions<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>
            <%= link_to "All created questions", questions_path, :class => "waves-effect waves-light btn-large" %>
                </li>
                <li>
            <%= link_to "create a new questions", new_question_path, :class => "waves-effect waves-light btn-large" %>
                </li>
            </ul>
            </li>
        <% end %>
        <% end %>
         <li class="dropdown">
            <a href="bootstrap-elements.html" data-target="#" class="dropdown-toggle" data-toggle="dropdown">User session <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>
                <% if user_signed_in? %>
                <li>
                <%= link_to 'Logout', destroy_user_session_path, :method => :delete %>
                </li>
            <% else %>
                <li>
                <%= link_to 'Login', new_user_session_path %>
                </li>
            <% end %>
                </li>
            </ul>
            </li>
        </ul>
    </div>
</div>

